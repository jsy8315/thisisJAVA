package com.jsy;

public class kopo31 {
    public static void main(String[] args) {
        int weight = 77;
        int age1 = 7;
        int age2 = 17;
        int age = 35;
        String name = "스마트폰";
        
        if (weight == 60); //변수 weight의 값이 60과 같다
        if ((age1 + age2)*2 > 60); //변수 age1과 age2의 합계를 2배한 것이 60을 넘는다
        if ((age % 2) == 1); //변수 age가 홀수이다
        if (age % 2 == 0); //변수 age가 짝수이다
        boolean isOdd = age % 2 == 1;
        boolean isEven = !isOdd; // 기존 변수를 활용함으로써 실수줄이기 & 간단히 문제 해결 가능
        if (name.equals("스마트")); //변수 name에 저장된 문자열이 "스마트"와 같다
               
        }
        
    }
 

